apiVersion: v1
kind: Service
metadata:
  name: {{ include "sellevate-service.name" . }}
  labels:
    {{- include "sellevate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sellevate-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
