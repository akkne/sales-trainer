# Dependency directories
node_modules/
.pnp
.pnp.js

# Next.js
src/frontend/.next/
src/frontend/out/
src/frontend/.env*.local

# .NET build outputs
src/backend/bin/
src/backend/obj/
src/backend/api/bin/
src/backend/api/obj/
src/backend/tests/bin/
src/backend/tests/obj/
src/SalesTrainer.Tests/bin/
src/SalesTrainer.Tests/obj/
# Per-service projects (building-blocks, gateway, future services) under src/backend
src/backend/**/bin/
src/backend/**/obj/

# Environment files
.env
.env.local
.env.*.local

# Personal server deployment runbook (may contain IPs / notes / secrets) — local only
SERVER_SETUP.md

# ASP.NET environment-specific settings (may contain secrets)
appsettings.*.json
# Committed template for the Testing config
!appsettings.Testing.example.json

# Claude & design config (local only, not committed)
.claude/
.design/

# OMC runtime state (sessions, mission/tool state, project memory) — local only
.omc/

# Nimbalyst local workspace artifacts (automations runtime, outputs) — local only.
# The canonical task pipeline is documented in docs/TASK_WORKFLOW.md.
nimbalyst-local/

# Docker volumes (if mounted locally)
.postgres_data/
.mongo_data/
.redis_data/

# Local dev profile runtime (PIDs for scripts/dev-up.sh)
.local-run/

# IDE
.idea/
.vscode/
*.suo
*.user

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
# Except Next.js API route named "logs"
!src/frontend/app/api/logs/
!src/frontend/app/api/logs/**
.playwright-mcp/

docs/АРХИТЕКТУРА_МИКРОСЕРВИСОВ.md
RESUME_PROJECT.txt
