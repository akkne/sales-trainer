# Тело финального PR `feature/tenancy` → `main`

Этот файл подготовлен блоком 40.34, чтобы человеку не пришлось сочинять описание PR на 278
коммитов. **Сам PR агент не создавал и ветку не пушил** — почему именно, написано в
`docs/DONT_FORGET.md`, раздел «Финальный PR».

Ниже — две части: готовые команды и текст, который надо вставить в тело PR.

---

## Часть 1 — команды

```bash
cd /Users/artem/Sellevate/Repository

# 1. Убедиться, что мержится то, что проверялось
git checkout feature/tenancy
git log --oneline -1        # ожидается вершина этапа 40.34
git diff --stat main..feature/tenancy | tail -1

# 2. Прогнать проверки ещё раз — они занимают меньше минуты
dotnet build src/backend/Sellevate.sln
for p in src/backend/*/*.Tests/*.csproj; do
  dotnet test "$p" --filter "TestCategory!=Integration" --no-build
done
( cd src/frontend && npx tsc --noEmit && npx vitest run )
python3 scripts/tenancy-boundary-lint.py
python3 scripts/tenancy-pool-lint.py
python3 scripts/ai-provider-lint.py

# 3. Пуш
git push -u origin feature/tenancy

# 4. PR (тело — часть 2 этого файла)
gh pr create \
  --base main \
  --head feature/tenancy \
  --title "Phase 40 — multi-tenancy: organizations, content versioning, assignments, AI in the admin panel" \
  --body-file docs/TENANCY/RELEASE_PR_BODY.md
```

> `--body-file` ждёт файл с одним только телом. Либо создайте
> `docs/TENANCY/RELEASE_PR_BODY.md` из части 2, либо вставьте текст в веб-форму GitHub руками.

**Не прогоняйте `dotnet test` по солюшену целиком** — проверено в 40.34: он отрабатывает не все
проекты (один раз отчитался о 53 тестах одного проекта, второй раз — ни о чём). Настоящие 891
получаются только циклом по `*.Tests/*.csproj`, как в команде выше.

---

## Часть 2 — тело PR

<!-- скопировать всё, что ниже разделителя -->

---

## Что это

Phase 40 целиком: продукт из однопользовательского становится мультитенантным. Тенант —
**организация** (`Organization`), а не `Company` (`Company` был и остаётся CRM-сущностью
«компания-клиент, которой продают»).

278 коммитов, 996 файлов, +111 852 / −3 074, двадцать закрытых блоков роадмапа (40.1–40.34).

Полная сводка для раскатки и приёмки — **[`docs/TENANCY/PHASE_40_SUMMARY.md`](docs/TENANCY/PHASE_40_SUMMARY.md)**.
Читать её надо до мержа, а не после.

## Этапы

- **A (40.1–40.4)** — примитивы тенанта в `BuildingBlocks`, `organizationId` в конверте событий и
  outbox, инфраструктура Postgres RLS. Три независимых слоя изоляции: заголовок гейтвея → EF-фильтр
  → RLS.
- **B (40.5–40.9)** — `organization-service` как реестр тенантов; memberships; разделение ролей на
  платформенные и организационные; закрытая регистрация и инвайты; способ логина как настройка
  организации (шов под SSO); суперадминка платформы с имперсонацией и аудитом.
- **C (40.10–40.14)** — `organization_id` по всем сервисам, включая Mongo и Redis; аудит фоновых
  задач с объявленным режимом у каждой. Ревью безопасности этапа — 0 критичных.
- **D (40.15–40.19)** — иммутабельные версии уроков, прогресс, привязанный к версии; версионирование
  программы и зачислений; copy-on-write override'ы с очередью `stale`; профиль организации и
  параметризация контента.
- **E (40.21–40.26)** — задания «РОП → менеджеры». Завершение — **порог качества**, а не факт
  прохождения. Фан-аут по правилу аудитории, автоповторы, дашборд РОПа, непрохождение как рабочий
  сценарий.
- **F (40.27–40.33)** — ИИ в админке: чекпоинт перед генерацией, порог достаточности входа,
  интервью по профилю, петля «метрика → контент», пакетная адаптация тона, квоты и стоимость на
  организацию.

Не входит: **40.20** (разделение админки — ждёт дизайна), **40.30** (записи звонков — ждёт
юридического решения), **40.35/40.36** (`[SKIP]`).

## Проверка

Свежий прогон на вершине ветки:

| Проверка | Результат |
|----------|-----------|
| `dotnet build src/backend/Sellevate.sln` | 0 ошибок, 24 предупреждения |
| Юнит-тесты, 11 проектов | **891 passed, 4 skipped, 0 failed** |
| `npx tsc --noEmit` | 0 ошибок |
| `npx vitest run` | **353 / 353**, 49 файлов |
| `scripts/tenancy-boundary-lint.py` | clean |
| `scripts/tenancy-pool-lint.py` | clean |
| `scripts/ai-provider-lint.py` | clean |
| `dotnet ef migrations has-pending-model-changes` × 7 | расхождений нет |
| `dotnet format --verify-no-changes` (гейт CI) | зелёный |

## Что нужно знать до мержа

**1. Тестов на этапы D, E и F нет.** Правило №3 действовало всю фазу: новые тесты не писались.
Это ~71 000 строк, не покрытых ничем, кроме сборки и существующих тестов соседнего кода. 891
зелёный тест — это в основном этапы A–C и код, написанный до фазы.

**2. Гейт `codestyle-lint.py` красный и упадёт на этом PR.** 1216 нарушений против 88 на `main`;
почти все — `/// <summary>`, которые §9 CODESTYLE.md запрещает. Гейт был красным и до фазы (585
нарушений ещё на 40.6–40.8) — правило и код противоречат друг другу с самого начала. Решение
владельца: смягчить §9 либо один раз вычистить identity-service. Разбор — в `PHASE_40_SUMMARY.md` §6.

**3. Мерж ≠ раскатка.** В ветке 30+ EF-миграций по семи сервисам, пять раскаточных скриптов, два
Mongo-бэкфилла и одно жёсткое ограничение порядка (**ai-service раньше learning-service**).
Последовательность — `PHASE_40_SUMMARY.md` §2. Ничего из этого ни против какой базы не выполнялось.

**4. Ни один вызов LLM за этапы D/E/F не делался.** Все промпты этапа F и новый HTTP-шов
learning → ai (40.33) на живой модели не проверялись — `PHASE_40_SUMMARY.md` §3.

**5. RLS не включена ни в одном окружении.** Построены три слоя изоляции, работают два.

## Порядок действий после мержа

1. Прочитать `docs/TENANCY/PHASE_40_SUMMARY.md` §2 целиком.
2. Проверить права роли, под которой пойдут миграции (§2.0) — без этого миграции отработают
   «успешно», не сделав ничего.
3. Раскатать по шагам §2.1, не переставляя их местами.
4. Пройти чеклист 40.34 в `docs/TESTING/TENANCY.md` на **двух** организациях.
5. Первый живой прогон ИИ-функций этапа F — ожидать проблем с разбором ответа модели.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
